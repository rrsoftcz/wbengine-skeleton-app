; Custom PHP configuration for development
; Suppress deprecation warnings from legacy dependencies (e.g., Texy)

; Report all errors except deprecation notices
error_reporting = E_ALL & ~E_DEPRECATED

; Display errors in browser (development only)
display_errors = On

; Log errors to file
log_errors = On
